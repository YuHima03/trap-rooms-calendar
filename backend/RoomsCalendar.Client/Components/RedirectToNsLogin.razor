@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        base.OnInitialized();
        Navigation.NavigateTo("_oauth/login?redirect=/", true, true);
    }
}
